#include <stdio.h>


int main() {
    int a, b;

    scanf("%d " "%d", &a, &b);

    //if(0 <= a && a <= 100 && 0 <= b && b <= 100) {
        printf("%d", a + b);
        
        return 0;
    //}
} // bai nay ao dat gioi han ma khong them xet den
/* Dành cho bạn mới: Thuật ngữ giới hạn trong đề 
bài ám chỉ rằng các bộ test được dùng để chấm code 
của bạn nằm trong giới hạn đó. Tức lời giải của bạn
chỉ cần đảm bảo đúng hết các đầu vào thỏa mãn giới 
hạn của đề, bạn không cần quan tâm tới các đầu vào 
nằm ngoài giới hạn. 
*/
